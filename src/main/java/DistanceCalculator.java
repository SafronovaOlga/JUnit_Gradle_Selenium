public class DistanceCalculator{        // Класс для расчета стоимости в зависимости от расстояния    // Рассчитывает стоимость доставки в зависимости от расстояния.        public double calculateCost(double distance) {                if (distance > 30) {                        return 300;        } else if (distance > 10) {                        return 200;        } else if (distance > 2) {                        return 100;                    } else {            return 50;        }    }}